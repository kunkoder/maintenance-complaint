# Nama aplikasi Spring Boot
spring.application.name=report

# Port server
server.address=0.0.0.0
server.port=8001

app.base-url=http://192.168.6.197:8001

# Timeout sesi (dalam detik)
server.servlet.session.timeout=1800

# Konfigurasi database MySQL
spring.datasource.url=jdbc:mysql://localhost:3306/maintenance_report_1?serverTimezone=Asia/Riyadh&useUnicode=true&characterEncoding=utf8
spring.datasource.username=root
spring.datasource.password=password
spring.jpa.hibernate.ddl-auto=update

# Konfigurasi email (Gmail SMTP)
# Gmail SMTP with Port 465 (SSL)
spring.mail.host=smtp.gmail.com
spring.mail.port=465
spring.mail.username=ggomugo@gmail.com
spring.mail.password=sonuhyfqccusztsi

# Enable SSL (not STARTTLS)
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.ssl.enable=true

# Optional: Bypass SSL certificate issues (only for dev)
spring.mail.properties.mail.smtp.ssl.trust=smtp.gmail.com
spring.mail.properties.mail.smtp.ssl.checkserveridentity=false

# DevTools (development tools)
spring.devtools.restart.enabled=true
spring.devtools.livereload.enabled=true
spring.thymeleaf.cache=false